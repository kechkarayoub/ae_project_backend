test:
    coverage run manage.py test
    coverage html OR coverage report

translation:
    mekemessages:
        python manage.py makemessages --locale=fr --locale=en
        or
        python manage.py makemessages --all
    compilemessages:
        python manage.py compilemessages --locale=fr --locale=en
        or
        python manage.py compilemessages

after modificating code:
    sudo supervisorctl reload
    sudo systemctl reload nginx

change server name:
    sudo nano /etc/nginx/sites-enabled/django.conf



after restart aws instance:
    change server_name in /etc/nginx/sites-available/ae_project:
        sudo nano /etc/nginx/sites-available/my_project
    add server front name to ae_project_backend/app/production.py
        change CORS_ORIGIN_WHITELIST variable